# Vue3 项目快速开始指南

## 欢迎使用 KAMFU Tech 官网前端项目！

这是一个基于 Vue 3 + Vite + Tailwind CSS 构建的现代化企业官网项目。

## 📦 第一步：安装依赖

打开终端，进入 `frontend` 目录，运行：

```bash
cd frontend
npm install
```

这将安装所有必要的依赖包，包括：
- Vue 3 - 核心框架
- Vue Router - 路由管理
- Pinia - 状态管理
- Axios - HTTP 客户端
- Tailwind CSS - 样式框架
- Vue I18n - 国际化

## 🚀 第二步：启动开发服务器

安装完成后，运行：

```bash
npm run dev
```

几秒钟后，你会看到：

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

在浏览器中打开 `http://localhost:3000` 即可看到网站！

## 📁 项目结构说明

```
frontend/
├── src/
│   ├── pages/              # 页面组件
│   │   ├── HomePage.vue        # 首页
│   │   ├── ServicesPage.vue    # 服务页
│   │   ├── CaseStudiesPage.vue # 案例页
│   │   ├── AboutPage.vue       # 关于页
│   │   └── ContactPage.vue     # 联系页
│   │
│   ├── components/         # 可复用组件
│   │   ├── layout/            # 布局组件
│   │   │   ├── AppHeader.vue  # 顶部导航
│   │   │   └── AppFooter.vue  # 底部
│   │   └── common/            # 通用组件
│   │       ├── ServiceCard.vue
│   │       └── CaseStudyCard.vue
│   │
│   ├── router/             # 路由配置
│   │   └── index.js           # 所有页面路由
│   │
│   ├── stores/             # 状态管理
│   │   ├── app.js             # 全局状态
│   │   ├── services.js        # 服务数据
│   │   └── caseStudies.js     # 案例数据
│   │
│   ├── api/                # API 接口
│   │   ├── client.js          # Axios 配置
│   │   └── services.js        # API 方法
│   │
│   ├── i18n/               # 国际化
│   │   ├── index.js           # i18n 配置
│   │   └── locales/           # 语言文件
│   │       ├── zh-CN.json     # 简体中文
│   │       └── en.json        # 英文
│   │
│   └── assets/             # 静态资源
│       └── styles/            # 样式文件
│           └── main.css       # 主样式文件
```

## 🎨 主要功能

### 1. 路由系统
项目包含以下页面：
- `/` - 首页
- `/services` - 服务介绍
- `/case-studies` - 案例展示
- `/about` - 关于我们
- `/contact` - 联系方式

### 2. 响应式布局
所有页面都支持：
- 桌面端 (>1024px)
- 平板端 (768px-1024px)
- 移动端 (<768px)

### 3. 国际化 (i18n)
点击导航栏的语言切换按钮可以切换：
- 简体中文 (zh-CN)
- 英文 (en)

### 4. API 集成
已配置好 Axios 客户端，支持：
- 自动请求/响应拦截
- 统一错误处理
- 开发环境代理到后端 (localhost:8000)

## 🛠️ 开发工作流

### 修改页面内容

1. **编辑页面**：打开 `src/pages/` 下的对应文件
2. **保存文件**：Vite 会自动热更新，无需刷新浏览器
3. **查看效果**：浏览器立即显示更改

### 添加新组件

1. 在 `src/components/common/` 创建新的 `.vue` 文件
2. 使用 `<script setup>` 语法
3. 在需要的页面中引入使用

示例：
```vue
<script setup>
import MyComponent from '@/components/common/MyComponent.vue'
</script>

<template>
  <MyComponent />
</template>
```

### 修改样式

项目使用 Tailwind CSS，可以直接在模板中使用工具类：

```vue
<template>
  <div class="bg-blue-500 text-white p-4 rounded-lg">
    内容
  </div>
</template>
```

自定义样式在 `src/assets/styles/main.css` 中添加。

### 修改翻译文本

1. 打开 `src/i18n/locales/zh-CN.json` (中文)
2. 或 `src/i18n/locales/en.json` (英文)
3. 添加或修改文本
4. 在组件中使用 `t('key.path')`

## 📝 常用命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

## 🔧 环境配置

### 开发环境
默认连接到 `http://localhost:8000` 的后端 API

### 自定义 API 地址
创建 `.env.local` 文件：

```env
VITE_API_BASE_URL=http://your-api-url.com
```

## 🎯 下一步建议

1. **熟悉 Vue 3 Composition API**
   - [Vue 3 官方文档](https://cn.vuejs.org/)
   
2. **学习 Tailwind CSS**
   - [Tailwind 文档](https://tailwindcss.com/docs)
   
3. **了解 Vue Router**
   - [Vue Router 文档](https://router.vuejs.org/zh/)

4. **掌握 Pinia 状态管理**
   - [Pinia 文档](https://pinia.vuejs.org/zh/)

## 🐛 常见问题

### Q: 端口 3000 已被占用？
A: 修改 `vite.config.js` 中的端口号：
```js
server: {
  port: 3001, // 改为其他端口
}
```

### Q: 依赖安装失败？
A: 尝试清理缓存：
```bash
rm -rf node_modules
rm package-lock.json
npm install
```

### Q: 样式不生效？
A: 确保已正确导入 Tailwind：
- 检查 `src/assets/styles/main.css`
- 确认在 `src/main.js` 中导入了该文件

## 📞 获取帮助

如有问题，可以：
1. 查看 `README.md` 获取更详细信息
2. 查看项目中的注释
3. 联系项目负责人

## 🎉 开始开发吧！

现在你已经准备好开始开发了！试着修改 `src/pages/HomePage.vue` 看看效果吧。

祝开发愉快！ 🚀

